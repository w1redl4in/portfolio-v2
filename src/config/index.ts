export const config = {
  endpoint: String(process.env.PRISMIC_API_ENDPOINT),
  accessToken: String(process.env.PRISMIC_ACCESS_TOKEN),
  unsplashAccessKey: String(process.env.UNSPLASH_ACCESS_KEY),
};
